<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/stylesheet/style.css">
  <title>Basic CRUD</title>
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
  <div class= "w-full min-h-screen  bg-zinc-900 p-10 text-white">
    <a class= "text-blue-400 tracking-tighter text-1xl " href="/"><< Create New User</a>
    <h1 class="text-3xl text-indigo-500 tracking-tighter mt-10 mb-10 ">All Users </h1>
    <div class="Users flex flex-wrap gap-3">
      <% users.forEach((user) => { %>
        <div class="user w-75 h-100 bg-zinc-700 p-1  rounded-lg overflow-hidden">
          <div class="image w-full h-70 rounded-lg bg-zinc-200 item-center overflow-hidden">
            <img class="w-full h-full object-cover " src="<%= user.Image %>" alt="">
          </div>
          <h3 class=""><%= user.name %></h3>
          <h5 class="text-zinc-400 tracking-tighter"><%= user.email %></h5>
          <hr class="border-1 border-cyan-300 mt-4">
          <div class="flex justify-between mt-4 px-2 py-1">
            <a class="text-teal-600" href="">Edit</a>
            <a class="text-rose-600" href="/delete/<%= user._id %>">Delete</a>
          </div>
        </div>
      <% }) %>
    </div>
  </div>
</body>
</html>